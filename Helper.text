# requests library দিয়ে আমরা internet থেকে data আনতে পারি!
pip install requests    #if any erro comes then below one
python -m pip install requests

#anthropic Library Install করো
python -m pip install anthropic

 python-dotenv install করো
 python -m pip install python-dotenv
 
 .env file a 
 ANTHROPIC_API_KEY=sk-ant-api03-তোমার-আসল-key

LangChain install ও setup
python -m pip install langchain
python -m pip install langchain-anthropic
python -m pip install langchain-core

RAG Install করো
python -m pip install langchain-community
python -m pip install faiss-cpu
python -m pip install langchain-huggingface
python -m pip install sentence-transformers
python -m pip install langchain-text-splitters
python -m pip install langchain


to remove vector Store
Remove-Item -Recurse -Force vectorstore

for Web Scraping + AI
python -m pip install requests
python -m pip install beautifulsoup4
python -m pip install lxml

Day 23 — FastAPI + AI!
python -m pip install fastapi
python -m pip install uvicorn
python -m pip install python-multipart